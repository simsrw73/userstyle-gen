const assert = require('assert');
const userstyleGen = require('../index.js');

describe('userstyleGen', () => {
  it('has a test', () => {
    assert(false, 'userstyleGen should have a test');
  });
});
